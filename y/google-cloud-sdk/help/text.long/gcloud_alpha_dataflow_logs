NAME

      gcloud alpha dataflow logs - a group of subcommands for working with
         Dataflow logs

SYNOPSIS

      gcloud alpha dataflow logs COMMAND [GLOBAL-FLAG ...]

DESCRIPTION

      (ALPHA) A group of subcommands for working with Dataflow logs.

GLOBAL FLAGS

      Run $ gcloud help for a description of flags available to all commands.

COMMANDS

      COMMAND is one of the following:

      list-messages

         (ALPHA) Retrieve the logs from a specific job using the GetMessages
         RPC.

NOTES

      This command is in the Google Cloud SDK gcloud component. See installing
      components if it is not installed.

      This command is an internal implementation detail and may change or
      disappear without notice.

      This command is currently in ALPHA and may change without notice.

